To run the script, is necessary have the "Web Server for Chrome" extension and run it in the folder 
that containts all the files (so we can read the JSON file)
then open the index.html (in chrome) and introduce the inches.
